#!/usr/bin/env bash
# Sourced fragment (not a standalone hook) — sales/hooks/directive.sh composes
# this into the SessionStart banner. Owns the qualification-criteria facet.
SALES_QUALIFICATION_FRAGMENT="PRODUCES (qualification criteria): framework_used must be MEDDPICC (default) or BANT (named fallback for short-cycle/simple deals) — never silently omitted. Under MEDDPICC, 7 required fields (Metrics, Economic Buyer, Decision Criteria, Decision Process, Identify Pain, Champion, Verdict) must carry a value or explicit unknown/blocked marker; no required field may be silently absent. 2 optional fields (Paper Process, Competition — spec required:false) may be omitted, but if declared must carry a value, not a bare label. Economic Buyer and Champion must be named (not TBD) before an opportunity advances past initial qualification. Enforced by sales-qualification-meddpicc's PreToolUse gate. Kill switch: SALES_QUALIFICATION_GATE_OFF=1."

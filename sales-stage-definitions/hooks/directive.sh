#!/usr/bin/env bash
# Sourced fragment (not a standalone hook) — sales/hooks/directive.sh composes
# this into the SessionStart banner. Owns the stage-definitions facet.
SALES_STAGE_DEFINITIONS_FRAGMENT="PRODUCES (stage definitions): 5-7 stages, >=2 objective falsifiable exit criteria per stage stated as completed buyer actions in past tense (never rep judgment/activity verbs), each stage name a completed-action not a rep activity, a named next-stage handoff per exit. Enforced by sales-stage-definitions's PreToolUse gate. Kill switch: SALES_STAGE_DEFINITIONS_GATE_OFF=1."

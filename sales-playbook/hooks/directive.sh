#!/usr/bin/env bash
# Sourced fragment (not a standalone hook) — sales/hooks/directive.sh composes
# this into the SessionStart banner. Owns the sales-playbook facet.
SALES_PLAYBOOK_FRAGMENT="PRODUCES (sales playbook): five sections required — process overview, qualification framework, ICP/persona summary, objection-handling/competitive notes, metrics. Full messaging scripts/positioning copy/lead-gen content are out of scope (hand off to marketing) — reference marketing's assets by name/link, never duplicate inline. Enforced by sales-playbook's PreToolUse gate. Kill switch: SALES_PLAYBOOK_GATE_OFF=1."
